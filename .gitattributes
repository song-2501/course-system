# 统一换行符策略：仓库内一律以 LF 存储，检出时不转换。
# 避免 Windows 与类 Unix 协作时产生整文件换行差异。
* text=auto eol=lf

# 二进制文件不做换行处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
